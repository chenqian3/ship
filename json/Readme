sample.cc:
  jsoncppライブラリを使ってjsonファイルを読み込むサンプルプログラムです。
汎用なもので、今回のアノテーションファイルに特化したものではありません。

correct.cc:
 
各boxデータの組の

   "box": [
       {
         "ID": 1,
         "X": 555,
         "Y": 1205,
         "W": 339,
         "H": 90,　←ここ
           }　　　 ←ここ
       },

  "H": 数字 の後ろの','
　そのしたの '}' の行
は余分で、削除するためのプログラムです。
 
getbox.cc:
  JsonファイルからBounding Box情報を取り出すサンプルプログラムです。
  情報は、ShipInfoの構造体にあります．
　　#Jsonファイルの中にある画像ファイル名は正しくないものはかなりあるために、信頼できません。

showbox.cc,　　　　　　　　　　　　　　　　　 showbox1.cc:
  jsonファイルにある画像ファイル名を使う。　　　jsonファイルと同じ名前の画像ファイルを使う。
　Bounding Boxを表示するプログラムです。

movbox.cc
  showbox1.cc をベースに表示画面をa.aviに保存するプログラムです。
